[package]
name = "catapulte-outbound-cloudflare"
version.workspace = true
license.workspace = true
edition.workspace = true
rust-version.workspace = true
publish.workspace = true

[lints]
workspace = true

[dependencies]
anyhow = { workspace = true }
catapulte-domain = { path = "../../domain" }
# lettre is used purely as a MIME builder here (no smtp-transport feature): the
# rendered RFC822 bytes are what we hand to the Cloudflare Email binding.
lettre = { workspace = true }
reqwest = { workspace = true }

[dev-dependencies]
tokio = { workspace = true }
wiremock = "0.6"
